generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./foton.db"
}

model Book {
  id Int @id @default(autoincrement())
  title String
  subtitle String?
  author String
  description String
  pageCount Int?
  bookCover String
}